<%= simple_nested_form_for([:admin,@track,@course,@unit,@lesson]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.association :unit %>
    <%= f.input :lesson_plan %>
    <%= f.input :sequence %>
  </div>
  <br>
  <div class="form-actions">
    <%= f.button :submit, :class => "btn btn-primary" %>
    <%= link_to 'Cancelar',  [:admin,@track,@course,@unit], :class => "btn btn-danger" %>
  </div>
<% end %>
