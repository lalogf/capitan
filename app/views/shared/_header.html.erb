<% content_for :scripts do %>
  <script>
    $(".dropdown-toggle").dropdown();
  </script>
<% end %>
<header>
  <nav class="navbar navbar-default">
    <div class="container-fluid pr-0 pr-15-xs">
      <div class="navbar-header">
        <button type="button" 
                class="navbar-toggle collapsed" 
                data-toggle="collapse" 
                data-target="#menu-navbar-collapse-1" 
                aria-expanded="false">
          
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>        
        <%= link_to course_list_path, :class => "navbar-brand" do %>
        	<%= image_tag 'isotipo.png' %>
        	El capitán
        <% end %>
      </div>
      <div class="collapse navbar-collapse" id="menu-navbar-collapse-1">    
        <ul class="nav navbar-nav navbar-right">
          <li>
            <a class="hidden-xs">
              <%= current_user.full_name %>
            </a>
            <a class="hidden-lg hidden-md">
             Mi Perfil
            </a>
          </li>
          <li class="hidden-xs">
            <a href="#" class="dropdown-toggle" data-toggle=dropdown>
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <% if current_user.admin %>
                <li>
                  <%= link_to "Panel de administración", dashboard_path %>
                </li>
              <% end %>
              <li>
                <%= link_to('Salir', destroy_user_session_path, :method => :delete) %>
              </li>
            </ul>
          </li>
          <% if current_user.admin %>
            <li class="hidden-lg hidden-md">
              <%= link_to "Panel de administración", dashboard_path %>
            </li>
          <% end %>
          <li class="hidden-lg hidden-md">
            <%= link_to('Salir', destroy_user_session_path, :method => :delete) %>
          </li>
        </ul>
      </div>      
    </div>
  </nav>
</header>