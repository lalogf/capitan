<% content_for :scripts do %>
  <script>
    $(".dropdown-toggle").dropdown();
  </script>
<% end %>
<header>
  <nav class="navbar navbar-default">
    <div class="container pb-7">
      <div class="row pv-7">
        <div class="col-sm-4">
          <div class="navbar-header">
            <%= link_to image_tag("logo.svg",alt: "Laboratoria código que transforma", class: "logo-laboratoria-movil", width: "190"), root_path %>
            <button type="button"
                    class="navbar-toggle collapsed mr-0"
                    data-toggle="collapse"
                    data-target="#menu-navbar-collapse-1"
                    aria-expanded="false">
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
          </div>
          <div class="collapse navbar-collapse" id="menu-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
              <% if current_user.student? || current_user.assistant? || current_user.teacher? || current_user.admin? %>
                <li>
                  <%= link_to('Mi Perfil', myprofile_path, :method => :get) %>
                </li>
                <% unless current_page?(codereview_path) %>
                  <li>
                    <%= link_to('Code Review', codereview_path, :method => :get) %>
                  </li>
                <% else %>
                  <li>
                    <%= link_to('Mis Cursos', root_path, :method => :get) %>
                  </li>
                <% end %>
              <% end %>
              <% if current_user.assistant? || current_user.teacher? || current_user.admin? %>
                <li>
                  <%= link_to("Panel de administración", dashboard_path, :method => :get) %>
                </li>
              <% end %>
              <li>
                <%= link_to('Salir', destroy_user_session_path, :method => :delete) %>
              </li>
            </ul>
          </div>
        </div>
        <div class="col-sm-4 text-center hidden-xs">
          <%= link_to image_tag("logo.svg",alt: "Laboratoria código que transforma", class: "logo-laboratoria", width: "190"), root_path %>
        </div>
        <div class="dropdown-container col-md-4 pt-7">
          <div class="collapse navbar-collapse in">
            <ul class="nav navbar-nav navbar-right">
              <li class="header-arrow">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                  <%= current_user.full_name %>
                  <%= image_tag "arrow-up.png",alt: "Arrow up", class: "minh-icon-arrow arrow up hide", data: { el: "down" } %>
                  <%= image_tag "arrow-down.png",alt: "Arrow down", class: "minh-icon-arrow arrow down", data: { el: "up" } %>
                </a>
                <ul class="dropdown-menu">
                  <% if current_user.student? || current_user.assistant? || current_user.teacher? || current_user.admin? %>
                    <li>
                      <%= link_to('Mi Perfil', myprofile_path, :method => :get) %>
                    </li>
                    <% unless current_page?(codereview_path) %>
                      <li>
                        <%= link_to('Code Review', codereview_path, :method => :get) %>
                      </li>
                    <% else %>
                      <li>
                        <%= link_to('Mis Cursos', root_path, :method => :get) %>
                      </li>
                    <% end %>
                  <% end %>
                  <% if current_user.assistant? || current_user.teacher? || current_user.admin? %>
                    <li>
                      <%= link_to("Panel de administración", dashboard_path, :method => :get) %>
                    </li>
                  <% end %>
                  <li>
                    <%= link_to('Salir', destroy_user_session_path, :method => :delete) %>
                  </li>
                </ul>
              </li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </nav>
</header>
